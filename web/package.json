{
  "name": "cvefloodline-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "copy-data": "node scripts/copy-data.mjs",
    "make-og": "node scripts/make-og.mjs",
    "predev": "npm run copy-data && npm run make-og",
    "dev": "vite",
    "prebuild": "npm run copy-data && npm run make-og",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "autoprefixer": "^10.5.2",
    "postcss": "^8.4.38",
    "sharp": "^0.35.2",
    "tailwindcss": "^4.3.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
